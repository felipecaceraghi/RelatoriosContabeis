# Example frontend environment variables
# If your backend runs on the same origin (Next dev proxy) you can leave this blank.
# Otherwise set this to the full backend URL, e.g. http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
